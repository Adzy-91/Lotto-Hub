<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/app_background">

    <android.support.v7.widget.RecyclerView
        style="@style/AppPadding"
        android:id="@+id/recyclerViewPrizes"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingTop="16dp"
        android:paddingBottom="16dp"
        android:background="@color/app_background"
        android:clipToPadding="false"
        android:visibility="gone"
        android:scrollbars="vertical"
        android:scrollbarStyle="outsideOverlay">
    </android.support.v7.widget.RecyclerView>

    <ProgressBar
        android:id="@+id/progressBarPrizeFragment"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"/>

    <TextView
        style="@style/ProgressBarText"
        android:id="@+id/progressBarTextPrizeFragment"
        android:text="@string/progress_bar_prize_fragment"
        android:layout_below="@id/progressBarPrizeFragment"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="8dp"
        />

</RelativeLayout>

